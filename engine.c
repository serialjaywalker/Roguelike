enum directions {up, left, right, down};

void readflags(unsigned char *flags){
	//takes in flags for buttons then clears them (pass by pointer)
	
	
	flags=0;
}

void move(unsigned char direction){
	switch(direction){
		case up:
			break;
		case left:
			break;
		case right:
			break;
		case down:
			break;
	}
	
}

